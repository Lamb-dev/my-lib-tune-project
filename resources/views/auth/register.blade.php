@extends('layouts.app')
@section('title','Create account')
@section('content')<div class="auth-page"><div class="auth-card"><p class="eyebrow">JOIN THE LIBRARY</p><h1>Make the shelves<br><em>yours.</em></h1><p class="auth-sub">Create a reader profile to save books, rate stories, and pick up exactly where you left off.</p><form method="POST" action="{{ route('register') }}" class="form-stack">@csrf<label>Username<input name="username" value="{{ old('username') }}" required></label>@error('username')<small class="error">{{ $message }}</small>@enderror<label>Email<input type="email" name="email" value="{{ old('email') }}" required></label>@error('email')<small class="error">{{ $message }}</small>@enderror<label>Password<input type="password" name="password" required></label><label>Confirm password<input type="password" name="password_confirmation" required></label>@error('password')<small class="error">{{ $message }}</small>@enderror<button class="button button-dark button-full">Create my account</button></form><p class="auth-foot">Already a reader? <a href="{{ route('login') }}">Sign in</a></p></div></div>@endsection
