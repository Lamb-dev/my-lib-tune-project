@extends('layouts.app')
@section('title','Sign in')
@section('content')<div class="auth-page"><div class="auth-card"><p class="eyebrow">WELCOME BACK</p><h1>Return to your<br><em>reading room.</em></h1><p class="auth-sub">Sign in to keep your ratings, saved books, and reading progress with you.</p><form method="POST" action="{{ route('login') }}" class="form-stack">@csrf<label>Email<input type="email" name="email" value="{{ old('email') }}" required autofocus></label>@error('email')<small class="error">{{ $message }}</small>@enderror<label>Password<input type="password" name="password" required></label>@error('password')<small class="error">{{ $message }}</small>@enderror<div class="form-row"><label class="check"><input type="checkbox" name="remember"> Remember me</label></div><button class="button button-dark button-full">Sign in</button></form><p class="auth-foot">New to Lib-Tune? <a href="{{ route('register') }}">Create an account</a></p></div></div>@endsection
